# AxVisor Guest Registry

This repo is temporarily moved to [the `registry` directory of `arceos-hypervisor/axvisor-guest`](https://github.com/arceos-hypervisor/axvisor-guest/tree/main/registry). This repo will be re-activated after the release CI of guests become stable.
